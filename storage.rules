rules_version = '2';

// Firebase Storage Rules for Kinesa/Grandmind App
service firebase.storage {
  match /b/{bucket}/o {

    // Progress Photos - users can only access their own photos
    match /progress_photos/{userId}/{fileName} {
      // Allow read if authenticated and user matches
      allow read: if request.auth != null && request.auth.uid == userId;

      // Allow write (create/update) if authenticated, user matches, and file is valid
      allow write: if request.auth != null
                   && request.auth.uid == userId
                   && request.resource.size < 10 * 1024 * 1024  // 10MB limit
                   && request.resource.contentType.matches('image/.*');  // Only images

      // Allow delete if authenticated and user matches
      allow delete: if request.auth != null && request.auth.uid == userId;
    }

    // User profile photos
    match /user_profiles/{userId}/{fileName} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
                   && request.auth.uid == userId
                   && request.resource.size < 5 * 1024 * 1024  // 5MB limit
                   && request.resource.contentType.matches('image/.*');
      allow delete: if request.auth != null && request.auth.uid == userId;
    }

    // Default deny all
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
